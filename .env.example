# Forum 项目环境变量配置模板
# 复制此文件为 .env 并修改相应的值

# ===========================================
# 数据库配置
# ===========================================
# MySQL 数据库连接信息
DB_PASSWORD=change_me_strong_password
DB_DATABASE=forum
DB_USERNAME=root
DB_PORT=3306

# ===========================================
# API 服务配置
# ===========================================
# API 服务端口
API_PORT=4000

# JWT 认证配置 (生产环境请使用复杂密钥)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
JWT_EXPIRATION_MINUTES=60
JWT_REFRESH_EXPIRATION_DAYS=7
JWT_ISSUER=ForumApi
JWT_AUDIENCE=ForumClient

# CORS 允许的域名 (多个域名用逗号分隔)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ===========================================
# 前端配置
# ===========================================
# 前端应用端口
FRONTEND_PORT=3000

# ===========================================
# Redis 配置
# ===========================================
# Redis 缓存端口
REDIS_PORT=6379

# ===========================================
# 开发工具配置
# ===========================================
# phpMyAdmin 数据库管理工具端口
PHPMYADMIN_PORT=8080

# ===========================================
# 安全提示
# ===========================================
# 1. 请将 DB_PASSWORD 改为强密码
# 2. JWT_SECRET 在生产环境中必须使用复杂的密钥 (至少32位)
# 3. 不要将 .env 文件提交到版本控制系统
# 4. 生产环境建议使用更安全的密钥管理方案